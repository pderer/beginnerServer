import express from "express";

const rootRouter = express.Router();

/* rootRouter.get("/", (req, res) => {
  console.log("root success");
  res.send("root get success");
}); */

export default rootRouter;
